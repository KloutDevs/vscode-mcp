## What & why

<!-- What does this change do, and why? Link an issue if one exists (Fixes #123). -->

## How was this tested?

<!-- `npm run build && node --test dist/*.test.js` in root and/or extension/, and/or manual steps against a live Cursor instance. -->

## Checklist

- [ ] `npm run build` passes (root and/or `extension/`, whichever this PR touches)
- [ ] `node --test dist/*.test.js` passes
- [ ] `CHANGELOG.md` updated under `[Unreleased]` (for user-facing changes)
- [ ] If this touches `src/cdp.ts` or `src/composerStore.ts`: which Cursor version did you test against?
